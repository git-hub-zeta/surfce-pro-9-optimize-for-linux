# surfce-pro-9-optimize-for-ubuntu
让ubutnu更舒适使用

Optimize System to make ubuntu more comfortable to use

我的版本是surface内核的ubuntu24的lts

My version is the Ubuntu 24 LTS with the Surface kernel. 

1.对于GNOME shell优化触控制后，用给出的脚本可以在连接键盘的时候禁用虚拟键盘，在键盘拔出之后启用 

1. For GNOME shell optimized touch control, you can use the provided script to disable the virtual keyboard when a keyboard is connected, and enable it after the keyboard is unplugged.
